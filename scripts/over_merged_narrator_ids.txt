# Canonical Narrator ids (nar:<uuid>) that graph-flag-over-merged.yml flags with
# over_merged=true — the "flag-now" leg of the honest-leaderboard work
# (deploy#603 / main#928 / main#958 / da#443).
#
# BOOLEAN ONLY: the live op sets over_merged=true on these ids; the per-node
# over_merge_note is NOT set live (free-text prose belongs in the typed parquet
# reload, which writes each node's own note — deploy#603 revision).
#
# AUTHORITATIVE SOURCE: the data-acquisition producer's resolution of the curated
# seed (da#447/da#448). Do NOT hand-derive these ids from narrator names —
# canonical ids drift across resolve runs (da#371/#376); only the producer's
# resolved ids are authoritative. The workflow flags EXACTLY these ids and refuses
# a real run unless every one of them is present in the live graph (a mismatch
# means the seed is stale against the loaded resolve run, not something to flag).
#
# Format: one canonical id per line; blank lines and #-comments are ignored.
nar:9b793066-94f6-5f99-ae1c-b2636515fa9b
nar:afc30d02-f7ed-5d09-bc3b-4535a27dc0fb
nar:5b41b11e-b18a-5c18-b6e9-2091cb7d3cdf
nar:cb879ada-0dac-575a-a071-ffe2d265129c
nar:0806983d-8571-5a6f-b22e-62647a71cd4c
nar:3f7c1e28-2632-5214-bbec-c17ac5c021a9
nar:49618f4b-7a1b-5c35-8c95-5b837b965857
nar:cf44624b-1f1b-5c9e-b498-625d55191fd2
